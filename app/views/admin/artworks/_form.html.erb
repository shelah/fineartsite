<%= semantic_form_for [:admin, @artwork], :html => { :multipart => true } do |f| %>
  <% if @artwork.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artwork.errors.count, "error") %> prohibited this artwork from being saved:</h2>

      <ul>
      <% @artwork.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%#  <div class="field"> %>
<%#      <%= f.label :series %>
<%# <br /> %>
<%#      <%= f.select_tag :series_id, options_from_collection_for_select(@series, "id", "name") %>
<%#    </div> %>
  <%= f.inputs :name => "Series" do %>
    <%= f.input :title %>
    <%= f.input :width %>
    <%= f.input :height %>
    <%= f.input :medium %>
    <%= f.input :year, :as => :date_select, :start_year => 1980, :end_year => Time.now.year, :discard_day => true %>
    <%= f.input :image, :label => 'Image file' %>
    <%= f.input :img_width, :label => 'Image width' %>
    <%= f.input :img_height, :label => 'Image height' %>
    <%= f.input :thumbnail %>
    <%= f.actions %>
  <% end %>
<% end %>
